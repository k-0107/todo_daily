name: todo_daily
description: "A new Flutter project."
publish_to: "none"
version: 1.0.0+1
environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  go_router:
  flutter_riverpod:
  flutter_hooks:
  riverpod_annotation:
  cloud_firestore:
  firebase_core: ^3.8.0
  google_fonts:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  build_runner:
  riverpod_generator:

flutter:
  uses-material-design: true
  assets:
    - assets/google_fonts/
